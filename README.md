## Crammer Education API

1. `git clone` the repo
2. `npm install` to install all of the packages
3. Create `.env` file with the credentials `PORT` and `DB_URL`
4. Run `npm run dev` to start the server
5. Server should run on port `3000`
