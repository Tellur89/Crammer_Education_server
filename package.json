{
	"name": "server",
	"version": "1.0.0",
	"description": "1. `git clone` the repo\r 2. `npm install` to install all of the packages\r 3. Create `.env` file with the credentials `PORT` and `DB_URL`\r 4. Run `npm run dev` to start the server\r 5. Server should run on port `3000`",
	"main": "index.js",
	"scripts": {
		"start": "node index.js",
		"dev": "nodemon index.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"cors": "^2.8.5",
		"dotenv": "^16.0.3",
		"express": "^4.18.2",
		"morgan": "^1.10.0"
	},
	"devDependencies": {
		"nodemon": "^2.0.22"
	}
}
